% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse.R
\name{rb_parse_sources}
\alias{rb_parse_sources}
\alias{rb_default_marker_patterns}
\alias{rb_extract_markers}
\alias{rb_parse_source_table}
\title{Parse and standardize source sequence tables}
\usage{
rb_default_marker_patterns()

rb_extract_markers(
  description,
  marker_patterns = rb_default_marker_patterns(),
  genome_pattern = "complete genome|mitochondrion, complete genome|mitogenome",
  multi_gene_pattern = paste0("genes for|gene for|and|;|,.*and|partial.*complete|",
    "complete.*partial|complete.*complete|rrna.*rrna|", "rrna.*trna|trna.*rrna"),
  other_gene_pattern = "trna|transfer rna|nd[0-9]|atp[0-9]|nad[0-9]"
)

rb_parse_source_table(
  raw,
  source_name,
  column_map,
  marker_fn = rb_extract_markers,
  description_col = NULL,
  min_length = 1
)
}
\arguments{
\item{description}{Free-text sequence description, for example a GenBank
definition line.}

\item{marker_patterns}{Named list mapping marker names to detection
regular expressions.}

\item{genome_pattern}{Regular expression identifying complete genome
records.}

\item{multi_gene_pattern}{Regular expression identifying multi-gene
records.}

\item{other_gene_pattern}{Regular expression identifying non-marker gene
mentions such as tRNA, ND, or ATP genes.}

\item{raw}{Raw data frame as read from a source file.}

\item{source_name}{Short label for the source, for example \code{"ncbi"} or
\code{"bold"}.}

\item{column_map}{Named character vector mapping standardized column
names to column names in \code{raw}.}

\item{marker_fn}{Function used to derive marker type from a description
column. Default is \code{rb_extract_markers()}.}

\item{description_col}{Optional name of a free-text description column
in \code{raw}.}

\item{min_length}{Minimum sequence length to retain. Default \code{1}.}
}
\value{
\itemize{
\item \code{rb_default_marker_patterns()} returns a named list of regular
expressions.
\item \code{rb_extract_markers()} returns a character string describing the
detected marker and completeness, for example \code{"COI_partial"},
\code{"12S_unknown"}, \code{"genome_complete"}, \code{"multi_marker"}, or
\code{"other_unknown"}.
\item \code{rb_parse_source_table()} returns a standardized data frame.
}
}
\description{
Functions to extract marker information from sequence descriptions and
parse raw source tables into the standardized barcurateR schema.
\itemize{
\item \code{rb_default_marker_patterns()}: Returns the default regular expressions
used to detect common mitochondrial and ribosomal markers.
\item \code{rb_extract_markers()}: Extracts marker type and completeness from a
free-text sequence description.
\item \code{rb_parse_source_table()}: Parses a raw source table into the
standardized schema used by downstream QC and curation functions.
}
}
\details{
The standardized schema used by barcurateR expects at least:
\itemize{
\item \code{species}
\item \code{sequence}
\item \code{seq_type}
\item \code{source}
}

Additional columns such as \code{sequence_id}, \code{description}, \code{length},
and source-specific metadata may also be retained.

\code{rb_parse_source_table()} uses a user-supplied \code{column_map} to rename
raw columns into the standardized schema. If a description column is
provided, marker type is inferred using \code{rb_extract_markers()} and then
simplified with the internal helper \code{rb_simplify_seq_type()}.
}
\examples{
\dontrun{
raw_ncbi <- read.csv("ncbi_sequences.csv", stringsAsFactors = FALSE)

parsed_ncbi <- rb_parse_source_table(
  raw_ncbi,
  source_name = "ncbi",
  column_map = c(
    sequence_id = "sequence_id",
    species = "species_query",
    sequence = "sequence"
  ),
  description_col = "description"
)
}

}
\seealso{
Other parsing:
\code{\link{rb_ambiguity_utils}},
\code{\link{rb_combine_resolve}},
\code{\link{rb_sequence_prep}}
}
\concept{parsing}
