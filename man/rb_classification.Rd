% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify.R
\name{rb_classification}
\alias{rb_classification}
\alias{rb_sequence_features}
\alias{rb_train_classifier}
\alias{rb_classify_sequences}
\title{Machine learning classification of DNA sequences}
\usage{
rb_sequence_features(sequence)

rb_train_classifier(
  data,
  label_col = "seq_type",
  features = c("length", "gc_content", "at_content", "gc_skew", "at_skew"),
  trees = 500,
  mtry = 3,
  min_n = 5,
  prop = 0.8,
  seed = 123
)

rb_classify_sequences(
  model_result,
  data,
  confidence_threshold = 0.8,
  min_length = 100,
  sequence_col = "sequence",
  fallback_label = "other"
)
}
\arguments{
\item{sequence}{A character vector of DNA sequences.}

\item{data}{A data frame containing the sequences and/or features.}

\item{label_col}{The name of the column in \code{data} containing the class labels (e.g., marker types like "12S", "COI").}

\item{features}{A character vector of column names to use as predictor features.}

\item{trees}{The number of trees in the Random Forest.}

\item{mtry}{The number of predictors randomly sampled at each split.}

\item{min_n}{The minimum number of data points in a terminal node.}

\item{prop}{The proportion of data to use for the training set (the rest is used for testing).}

\item{seed}{Random seed for reproducibility of the train/test split.}

\item{model_result}{The output list from \code{rb_train_classifier()}.}

\item{confidence_threshold}{Minimum prediction probability required to confidently assign a class.}

\item{min_length}{Minimum sequence length (in base pairs) required for classification.}

\item{sequence_col}{The name of the column in \code{data} containing the raw sequences.}

\item{fallback_label}{The label assigned when a sequence is too short or falls below the \code{confidence_threshold} (default \code{"other"}).}
}
\value{
\itemize{
\item \code{rb_sequence_features()} returns a data frame of computed features (\code{length}, \code{gc_content}, \code{at_content}, \code{gc_skew}, \code{at_skew}).
\item \code{rb_train_classifier()} returns a list containing the trained model (\code{workflows::workflow()} fit), performance metrics, and test set results.
\item \code{rb_classify_sequences()} returns the input \code{data} augmented with \code{predicted_class}, \code{confidence_score}, \code{final_type}, and \code{prediction_source} columns.
}
}
\description{
Functions to extract sequence features, train a Random Forest classifier,
and predict marker types for unknown sequences based on sequence composition.
\itemize{
\item \code{rb_sequence_features()}: Extracts numerical features (length, GC/AT content, GC/AT skew) from DNA sequences.
\item \code{rb_train_classifier()}: Trains a Random Forest model to classify sequences into marker types using the \code{tidymodels} framework.
\item \code{rb_classify_sequences()}: Predicts marker types for new sequences using a trained model, applying confidence and length filtering.
}
}
\concept{machine learning}
