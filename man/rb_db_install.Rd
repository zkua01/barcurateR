% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db-install.R
\name{rb_db_install}
\alias{rb_db_install}
\alias{rb_yzfishdb_release}
\alias{rb_default_yzfishdb_url}
\alias{rb_db_url}
\alias{rb_db_dir}
\alias{rb_db_path}
\alias{rb_db_available}
\alias{rb_install_db}
\title{Install and manage barcurateR reference databases}
\usage{
rb_yzfishdb_release()

rb_default_yzfishdb_url()

rb_db_url()

rb_db_dir(create = TRUE)

rb_db_path(filename = "YZFishDB.db")

rb_db_available(path = rb_db_path())

rb_install_db(
  url = rb_db_url(),
  destfile = rb_db_path(),
  overwrite = FALSE,
  md5 = NULL,
  sha256 = NULL,
  min_bytes = 1,
  quiet = FALSE,
  timeout = 3600,
  retries = 3
)
}
\arguments{
\item{create}{Logical. If \code{TRUE}, create the cache directory if it doesn't exist.}

\item{filename}{The name of the database file (default \code{"YZFishDB.db"}).}

\item{path}{Path to check for database availability.}

\item{url}{URL to download the database from.}

\item{destfile}{Destination file path for the download.}

\item{overwrite}{Logical. Overwrite existing file?}

\item{md5, sha256}{Expected checksums for verification.}

\item{min_bytes}{Minimum expected file size in bytes.}

\item{quiet}{Logical. Suppress download progress messages?}

\item{timeout}{Download timeout in seconds (default 3600).}

\item{retries}{Number of download retry attempts.}
}
\value{
\itemize{
\item \code{rb_db_dir()} and \code{rb_db_path()} return a character string path.
\item \code{rb_db_available()} returns a logical value.
\item \code{rb_install_db()} returns the normalized path to the downloaded file.
\item \code{rb_yzfishdb_release()} returns a data frame of release metadata.
}
}
\description{
Functions to locate, download, and manage cached reference databases.
\itemize{
\item \code{rb_db_dir()}: Returns the path to the local cache directory.
\item \code{rb_db_path()}: Returns the full path to a specific database file in the cache.
\item \code{rb_db_available()}: Checks if a database file exists locally.
\item \code{rb_db_url()}: Returns the configured download URL for the full database.
\item \code{rb_install_db()}: Downloads the full database from Zenodo (or a custom URL)
into the local cache, with retry logic and checksum verification.
\item \code{rb_yzfishdb_release()}: Returns metadata for the official YZFishDB Zenodo release.
\item \code{rb_default_yzfishdb_url()}: Returns the default Zenodo API URL for YZFishDB.
}
}
\details{
The cache directory is determined by:
\enumerate{
\item The \code{BARCURATER_CACHE_DIR} environment variable.
\item The default R user data directory (\code{tools::R_user_dir("barcurateR", "data")}).
}

The download URL is determined by:
\enumerate{
\item The \code{BARCURATER_DB_URL} environment variable.
\item The \code{barcurateR.db_url} global option.
\item The default YZFishDB Zenodo URL.
}
}
\seealso{
Other database management:
\code{\link[=rb_connect]{rb_connect()}}
}
\concept{database management}
