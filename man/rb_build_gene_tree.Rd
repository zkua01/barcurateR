% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phylogeny.R
\name{rb_build_gene_tree}
\alias{rb_build_gene_tree}
\title{Build a neighbor-joining gene tree for a marker}
\usage{
rb_build_gene_tree(
  data,
  gene_type,
  gene_col = "seq_type",
  species_col = "species",
  sequence_col = "sequence",
  id_col = "unique_code",
  outgroup = NULL,
  outgroup_exact = TRUE,
  dna_model = "K80",
  min_seqs = 3,
  min_length = 100
)
}
\arguments{
\item{data}{A standardized data frame containing at least the columns
specified by \code{gene_col}, \code{species_col}, \code{sequence_col}, and \code{id_col}.}

\item{gene_type}{The marker/gene to build the tree for (e.g., \code{"COI"},
\code{"12S"}).}

\item{gene_col}{Name of the column containing marker/gene labels.
Default \code{"seq_type"}.}

\item{species_col}{Name of the species column. Default \code{"species"}.}

\item{sequence_col}{Name of the sequence column. Default \code{"sequence"}.}

\item{id_col}{Name of the unique sequence identifier column.
Default \code{"unique_code"}.}

\item{outgroup}{Optional species name to root the tree on. If \code{NULL},
the tree is left unrooted.}

\item{outgroup_exact}{Logical. If \code{TRUE} (default), \code{outgroup} is matched
as an anchored prefix against tip labels (\code{"^<outgroup>-"}), avoiding
accidental partial matches. Set \code{FALSE} for substring-anywhere matching.}

\item{dna_model}{DNA substitution model passed to \code{ape::dist.dna()}.
Default \code{"K80"} (Kimura 2-parameter).}

\item{min_seqs}{Minimum number of sequences required to build a tree.
Default \code{3}.}

\item{min_length}{Minimum sequence length (in bp) after gap removal.
Default \code{100}.}
}
\value{
A list containing:
\describe{
\item{tree}{A rooted or unrooted \code{phylo} object.}
\item{method}{The tree-building method used (\code{"njs"}).}
\item{n_tips}{The number of tips in the tree.}
}
Returns \code{NULL} (with a warning) if fewer than \code{min_seqs} sequences
remain after filtering.
}
\description{
Builds a neighbor-joining phylogenetic tree for a single gene or marker
across all sequences in the dataset. Sequences are aligned with
\code{DECIPHER::AlignSeqs()}, pairwise distances are computed with
\code{ape::dist.dna()}, and the tree is constructed with \code{ape::njs()}.

Optionally, the tree can be rooted on a user-specified outgroup.
}
\details{
The function performs the following steps:
\enumerate{
\item Filters \code{data} to the requested \code{gene_type}.
\item Removes duplicate sequences.
\item Strips alignment gap characters (\code{"-"}).
\item Filters sequences shorter than \code{min_length}.
\item Aligns sequences using \code{DECIPHER::AlignSeqs()}.
\item Computes pairwise distances with \code{ape::dist.dna()}.
\item Builds a neighbor-joining tree with \code{ape::njs()}.
\item Optionally roots the tree on the specified outgroup.
}

Tip labels are formatted as \code{"<species>-<unique_code>"}, with spaces
in species names replaced by hyphens.

This function deliberately does not include taxon-specific mislabel
detection logic. For that, build on top of this function using
project-specific heuristics.
}
\examples{
\dontrun{
# Build an unrooted COI tree
tree_result <- rb_build_gene_tree(final_data, gene_type = "COI")
plot(tree_result$tree)

# Build a rooted 12S tree using an outgroup
tree_result <- rb_build_gene_tree(
  final_data,
  gene_type = "12S",
  outgroup = "Alligator sinensis"
)
plot(tree_result$tree)
}

}
\concept{phylogeny}
