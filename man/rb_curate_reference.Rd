% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/curate.R
\name{rb_curate_reference}
\alias{rb_curate_reference}
\title{Curate a DNA reference sequence database}
\usage{
rb_curate_reference(
  data,
  blast_db = NULL,
  numt_fasta = NULL,
  run_classifier = FALSE,
  run_divergence = FALSE,
  run_barcode_gap = FALSE,
  taxonomy_table = NULL,
  ambiguity_data = NULL,
  db_path = NULL,
  confidence_threshold = 0.8,
  min_length = 100,
  classifier_trees = 500,
  barcode_gap_markers = NULL,
  barcode_gap_min_seqs = 5,
  barcode_gap_parallel = FALSE,
  reference_table = "reference_final",
  qc_table = "qc_reference",
  barcode_gap_table = "barcode_gap_metrics",
  ambiguity_table = "ambiguous_sequences",
  ambiguity_path = "ambiguous_sequences.csv",
  archive_ambiguity = TRUE,
  check_ambiguity = TRUE,
  on_ambiguous = c("stop", "warn", "ignore"),
  require_taxonomy = TRUE,
  mafft = "mafft",
  fasttree = "FastTree"
)
}
\arguments{
\item{data}{A standardized data frame containing at least \code{species},
\code{sequence}, and \code{seq_type}. A \code{source} column is also recommended.}

\item{blast_db}{Path prefix to a pre-built BLAST database used for
contaminant screening.}

\item{numt_fasta}{Path to a FASTA file of known NUMT sequences.}

\item{run_classifier}{Logical. If \code{TRUE}, trains a Random Forest classifier
on known marker types and predicts marker types for sequences labeled
\code{"other"} or \code{"other_unknown"}.}

\item{run_divergence}{Logical. If \code{TRUE}, runs phylogenetic divergence
checks. Requires \code{mafft} and \code{FastTree} to be installed.}

\item{run_barcode_gap}{Logical. If \code{TRUE}, runs barcode gap analysis after
QC and taxonomy joining.}

\item{taxonomy_table}{Optional data frame used to join taxonomic lineage
onto the curated sequences. Must contain \code{species} and the seven
taxonomic ranks (kingdom through species).}

\item{ambiguity_data}{Optional data frame of ambiguity records to archive
in the SQLite database.}

\item{db_path}{Optional path to write the final SQLite database.}

\item{confidence_threshold}{Minimum prediction probability required for
the ML classifier to reclassify an unknown sequence.}

\item{min_length}{Minimum sequence length required for ML classification.}

\item{classifier_trees}{Number of trees used by the Random Forest classifier.}

\item{barcode_gap_markers}{Optional character vector of markers to include
in barcode gap analysis. If \code{NULL}, all markers except \code{"other"} are used.}

\item{barcode_gap_min_seqs}{Minimum number of sequences per species
required for barcode gap analysis.}

\item{barcode_gap_parallel}{Logical. If \code{TRUE}, runs barcode gap analysis
in parallel where supported.}

\item{reference_table}{Name of the final reference table written to the
SQLite database. Default \code{"reference_final"}.}

\item{qc_table}{Name of the QC table. Default \code{"qc_reference"}.}

\item{barcode_gap_table}{Name of the barcode gap table. Default \code{"barcode_gap_metrics"}.}

\item{ambiguity_table}{Name of the ambiguity table. Default \code{"ambiguous_sequences"}.}

\item{ambiguity_path}{Path to a CSV file containing ambiguity records.
If \code{ambiguity_data = NULL}, \code{archive_ambiguity = TRUE}, and this file
exists, it will be read and archived.}

\item{archive_ambiguity}{Logical. If \code{TRUE}, attempts to archive ambiguity
records in the SQLite database.}

\item{check_ambiguity}{Logical. If \code{TRUE}, checks the input data for
sequences assigned to multiple species before running QC.}

\item{on_ambiguous}{Action to take if ambiguous input sequences are found.
One of \code{"stop"}, \code{"warn"}, or \code{"ignore"}.}

\item{require_taxonomy}{Logical. If \code{TRUE}, requires the final curated
table to contain full kingdom-to-species taxonomy.}

\item{mafft}{Path to the \code{mafft} executable.}

\item{fasttree}{Path to the \code{FastTree} executable.}
}
\value{
An invisible list containing:
\describe{
\item{qc1}{Data frame after first-pass QC.}
\item{classifier}{Trained ML classifier object, or \code{NULL}.}
\item{qc2}{Data frame after second-pass QC.}
\item{final_data}{Final curated data frame.}
\item{barcode_gap}{Barcode gap metrics, or \code{NULL}.}
\item{db_path}{Path to the SQLite database, or \code{NULL}.}
}
}
\description{
\code{rb_curate_reference()} is the main user-facing wrapper that orchestrates
the full reference database curation pipeline:
\enumerate{
\item First-pass quality control (contaminants, NUMTs, codons, rRNA).
\item Optional machine-learning classification of unknown sequences.
\item Second-pass quality control using updated marker labels.
\item Optional taxonomic lineage joining and validation.
\item Optional barcode gap analysis.
\item Optional SQLite database export.
}
}
\details{
If \code{check_ambiguity = TRUE}, the wrapper checks whether the same sequence
is assigned to multiple species before QC begins. Such ambiguities should
normally be resolved during standardization using \code{rb_resolve_ambiguous()}.
}
\examples{
\dontrun{
curated <- rb_curate_reference(
  data = standardized_data,
  taxonomy_table = taxonomy_table,
  db_path = tempfile(fileext = ".db")
)
}

}
\seealso{
\code{rb_run_qc_pipeline()}, \code{rb_build_reference_db()}
}
\concept{curation}
