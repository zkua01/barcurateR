% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connect.R
\name{rb_connect}
\alias{rb_connect}
\alias{rb_available_datasets}
\alias{rb_disconnect}
\alias{rb_tables}
\alias{rb_read_table}
\title{Connect to and manage barcurateR SQLite databases}
\usage{
rb_available_datasets()

rb_connect(path = NULL, download = FALSE, url = rb_db_url())

rb_disconnect(con)

rb_tables(con)

rb_read_table(con, table)
}
\arguments{
\item{path}{Path to a SQLite database file. If \code{NULL}, the default
bundled demo database is used.}

\item{download}{Logical. If \code{TRUE} and the database file does not exist,
attempt to download it from the default URL.}

\item{url}{The URL to download the database from (used when \code{download = TRUE}).}

\item{con}{A \code{DBIConnection} object returned by \code{rb_connect()}.}

\item{table}{The name of the table to read.}
}
\value{
\itemize{
\item \code{rb_connect()} returns a \code{DBIConnection} object (S4 class \code{SQLiteConnection}).
\item \code{rb_disconnect()} returns \code{TRUE} invisibly.
\item \code{rb_tables()} returns a data frame with columns \code{table} and \code{n_rows}.
\item \code{rb_read_table()} returns a data frame containing the table contents.
\item \code{rb_available_datasets()} returns a data frame with columns \code{name}, \code{version}, and \code{path}.
}
}
\description{
Functions to connect to, inspect, and manage barcurateR SQLite reference
databases. These functions provide the primary interface for interacting
with curated reference sequence databases.
\itemize{
\item \code{rb_connect()}: Opens a connection to a SQLite database.
\item \code{rb_disconnect()}: Safely closes an active database connection.
\item \code{rb_tables()}: Lists all tables in the database with row counts.
\item \code{rb_read_table()}: Reads an entire table into a data frame.
\item \code{rb_available_datasets()}: Lists available bundled or local databases.
}
}
\details{
The default database resolution order in \code{rb_connect()} when \code{path = NULL} is:
\enumerate{
\item If \code{download = TRUE} and no cached file exists, download from \code{rb_db_url()}.
\item Otherwise, use the first path from \code{rb_available_datasets()}.
}
}
\examples{
\dontrun{
# Connect to the bundled demo database
con <- rb_connect()
rb_tables(con)

# Read a specific table
refs <- rb_read_table(con, "reference_final")

# Close the connection
rb_disconnect(con)
}

}
\seealso{
Other database management:
\code{\link{rb_db_install}}
}
\concept{database management}
