% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse.R
\name{rb_combine_resolve}
\alias{rb_combine_resolve}
\alias{rb_combine_sources}
\alias{rb_resolve_ambiguous}
\title{Combine parsed sources and resolve ambiguous sequences}
\usage{
rb_combine_sources(
  source_list,
  species_col = "species",
  sequence_col = "sequence"
)

rb_resolve_ambiguous(
  data,
  species_col = "species",
  sequence_col = "sequence",
  resolution_table = NULL,
  on_unresolved = "stop",
  flag_output_path = NULL
)
}
\arguments{
\item{source_list}{List of standardized source data frames.}

\item{species_col}{Name of the species column. Default \code{"species"}.}

\item{sequence_col}{Name of the sequence column. Default \code{"sequence"}.}

\item{data}{Standardized data frame to check for ambiguous sequences.}

\item{resolution_table}{Optional data frame resolving ambiguous
sequences. Must contain an \code{action} column with values \code{"keep"},
\code{"combine"}, or \code{"drop"}.}

\item{on_unresolved}{Action to take when ambiguous sequences are found
and no \code{resolution_table} is supplied. One of \code{"stop"}, \code{"warn"},
or \code{"drop"}. Default \code{"stop"}.}

\item{flag_output_path}{Optional path to write ambiguous rows for manual
review before resolving.}
}
\value{
\itemize{
\item \code{rb_combine_sources()} returns a combined data frame with duplicated
species-sequence combinations removed.
\item \code{rb_resolve_ambiguous()} returns a resolved data frame.
}
}
\description{
Functions to combine multiple standardized source tables and resolve
sequences that are assigned to multiple species.
\itemize{
\item \code{rb_combine_sources()}: Combines parsed source tables and removes
duplicated species-sequence combinations.
\item \code{rb_resolve_ambiguous()}: Detects and resolves sequences assigned to
multiple species.
}
}
\details{
Ambiguous sequences are sequences that appear under more than one
species. These must be resolved before QC because downstream QC and
curation assume that each sequence is assigned to a single species.

If a \code{resolution_table} is supplied, it should contain one row per
ambiguous sequence and an \code{action} column:
\itemize{
\item \code{"keep"}: keep the specified resolution.
\item \code{"combine"}: combine records across species, pipe-joining values that
differ across the ambiguous group.
\item \code{"drop"}: remove the ambiguous sequence.
}

If no \code{resolution_table} is supplied, \code{on_unresolved} controls the
behavior:
\itemize{
\item \code{"stop"}: stop with an error.
\item \code{"warn"}: warn and return the data unchanged.
\item \code{"drop"}: warn and remove ambiguous rows.
}
}
\examples{
\dontrun{
combined <- rb_combine_sources(
  list(ncbi_parsed, bold_parsed),
  species_col = "species",
  sequence_col = "sequence"
)

resolved <- rb_resolve_ambiguous(
  combined,
  on_unresolved = "drop"
)
}

}
\seealso{
Other parsing:
\code{\link{rb_ambiguity_utils}},
\code{\link{rb_parse_sources}},
\code{\link{rb_sequence_prep}}
}
\concept{parsing}
