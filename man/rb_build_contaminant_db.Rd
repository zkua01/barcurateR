% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contaminants.R
\name{rb_build_contaminant_db}
\alias{rb_build_contaminant_db}
\alias{rb_build_blast_db}
\title{Build contaminant reference databases for QC screening}
\usage{
rb_build_contaminant_db(
  organisms,
  out_dir,
  retmax = 200,
  requests_per_second = 3
)

rb_build_blast_db(
  fasta_paths,
  out_prefix,
  title = "Contaminant Database",
  makeblastdb = "makeblastdb",
  stream_combine = FALSE
)
}
\arguments{
\item{organisms}{A data frame with columns:
\itemize{
\item \code{type}: Either \code{"accession"} (direct \code{entrez_fetch}) or \code{"query"}
(\code{entrez_search} then fetch).
\item \code{category}: Output grouping that becomes the FASTA filename
(e.g., \code{"human_mtDNA"}, \code{"lab_bacteria"}, \code{"numt_source"}).
\item \code{value}: The accession ID (for \code{type = "accession"}) or NCBI
search query string (for \code{type = "query"}).
}}

\item{out_dir}{Directory where output FASTA files will be saved.
Created automatically if it does not exist.}

\item{retmax}{Maximum number of sequences to retrieve per NCBI search.}

\item{requests_per_second}{Throttle rate between NCBI requests.
The unauthenticated E-utilities limit is approximately 3 requests
per second.}

\item{fasta_paths}{Character vector of FASTA file paths to combine
and build into a BLAST database.}

\item{out_prefix}{Output prefix for the BLAST database files
(e.g., \code{"data/contaminants/contaminant_db"}).}

\item{title}{Title string embedded in the BLAST database metadata.}

\item{makeblastdb}{Path to the \code{makeblastdb} executable.}

\item{stream_combine}{Logical. If \code{TRUE}, combines files via chunked
streaming instead of loading them fully into memory. Recommended
for large contaminant sets.}
}
\value{
\itemize{
\item \code{rb_build_contaminant_db()} returns a named character vector of
output FASTA file paths, one per category.
\item \code{rb_build_blast_db()} returns the \code{out_prefix} path invisibly.
}
}
\description{
Functions to download contaminant sequences from NCBI and build
nucleotide BLAST databases for screening reference sequences against
known contaminants (e.g., human mtDNA, lab bacteria, NUMTs).
\itemize{
\item \code{rb_build_contaminant_db()}: Downloads sequences from NCBI based on
an organisms table, saving one FASTA file per category.
\item \code{rb_build_blast_db()}: Combines FASTA files and builds a nucleotide
BLAST database using \code{makeblastdb}.
}
}
\details{
The typical workflow is:
\enumerate{
\item Define an \code{organisms} table specifying what to download.
\item Call \code{rb_build_contaminant_db()} to download FASTA files.
\item Call \code{rb_build_blast_db()} on the relevant FASTA files to create
the BLAST database.
}

Note: NUMT sequences are typically screened via exact substring
matching (\code{rb_screen_numts()}) rather than BLAST, so NUMT FASTA files
are usually excluded from the BLAST database.
}
\examples{
\dontrun{
# Define organisms to download
organisms <- data.frame(
  type = c("accession", "query"),
  category = c("human_mtDNA", "lab_bacteria"),
  value = c(
    "NC_012920.1",
    "(Escherichia coli[ORGN] OR Pseudomonas[ORGN]) AND 16S[GENE]"
  ),
  stringsAsFactors = FALSE
)

# Download contaminant sequences
fasta_paths <- rb_build_contaminant_db(organisms, out_dir = "data/contaminants")

# Build BLAST database (excluding NUMTs if present)
blast_paths <- fasta_paths[!grepl("numt", names(fasta_paths))]
rb_build_blast_db(blast_paths, out_prefix = "data/contaminants/contaminant_db")
}

}
\concept{contaminant screening}
