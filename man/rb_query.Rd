% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query.R
\name{rb_query}
\alias{rb_query}
\alias{rb_get_sequences}
\alias{rb_list_taxa}
\alias{rb_filter_reference}
\alias{rb_taxonomy}
\title{Query and filter a curated reference database}
\usage{
rb_get_sequences(
  con,
  species = NULL,
  genus = NULL,
  family = NULL,
  marker = NULL,
  source = NULL,
  occurrence = NULL,
  qc_flag = "pass",
  exact = FALSE,
  table_name = "reference_final"
)

rb_list_taxa(
  con,
  rank = "species",
  occurrence = NULL,
  marker = NULL,
  qc_flag = "pass",
  exact = FALSE,
  table_name = "reference_final"
)

rb_filter_reference(
  data,
  marker = NULL,
  source = NULL,
  occurrence = NULL,
  min_length = NULL,
  max_length = NULL,
  qc_flag = "pass",
  exact = FALSE
)

rb_taxonomy(
  con,
  species = NULL,
  qc_flag = "pass",
  exact = FALSE,
  table_name = "reference_final"
)
}
\arguments{
\item{con}{A \code{DBIConnection} object to an open barcurateR SQLite database.}

\item{species, genus, family}{Optional character vectors to filter by
taxonomic rank.}

\item{marker}{Optional character vector of marker types to filter by
(e.g., \code{"COI"}, \code{"12S"}). Maps to the \code{seq_type} column.}

\item{source}{Optional character vector of source names to filter by
(e.g., \code{"ncbi"}, \code{"bold"}).}

\item{occurrence}{Optional character vector of occurrence categories
to filter by (e.g., \code{"native"}, \code{"introduced"}).}

\item{qc_flag}{QC flag filter. Default \code{"pass"}. When \code{exact = FALSE},
non-\code{"pass"} values are matched using SQL \verb{LIKE '\%value\%'}. When
\code{exact = TRUE}, exact equality is used. Set to \code{NULL} to skip
QC filtering.}

\item{exact}{Logical. If \code{TRUE}, use exact matching for \code{qc_flag}
filters. If \code{FALSE} (default), use pattern matching for non-\code{"pass"}
flags.}

\item{table_name}{Name of the reference table to query.
Default \code{"reference_final"}.}

\item{rank}{Taxonomic rank for \code{rb_list_taxa()}. One of \code{"kingdom"},
\code{"phylum"}, \code{"class"}, \code{"order"}, \code{"family"}, \code{"genus"}, or
\code{"species"}. Default \code{"species"}.}

\item{data}{A data frame containing reference sequences (for
\code{rb_filter_reference()}).}

\item{min_length, max_length}{Optional minimum and maximum sequence
length filters for \code{rb_filter_reference()}.}
}
\value{
\itemize{
\item \code{rb_get_sequences()} returns a data frame of reference sequences
matching the filter criteria.
\item \code{rb_list_taxa()} returns a data frame with columns for the requested
rank and \code{n_sequences}.
\item \code{rb_filter_reference()} returns a filtered data frame.
\item \code{rb_taxonomy()} returns a data frame of distinct taxonomy records
with all seven ranks plus optional \code{occurrence} and \code{habitat} columns.
}
}
\description{
Functions to retrieve, filter, and inspect reference sequences from a
barcurateR SQLite database or an in-memory data frame.
\itemize{
\item \code{rb_get_sequences()}: Retrieves sequences from a SQLite database with
optional taxonomic, marker, source, occurrence, and QC filters.
\item \code{rb_list_taxa()}: Lists taxa at a given taxonomic rank with sequence
counts.
\item \code{rb_filter_reference()}: Filters an in-memory reference data frame
using the same criteria as the database query functions.
\item \code{rb_taxonomy()}: Retrieves distinct taxonomy records from the database.
}
}
\details{
All database query functions use the generic table name
\code{"reference_final"} by default. This can be overridden via the
\code{table_name} parameter.

\code{rb_get_sequences()} and \code{rb_list_taxa()} filter on \code{qc_flag = 'pass'}
by default, ensuring only quality-checked sequences are returned.
Set \code{qc_flag = NULL} to retrieve all sequences regardless of QC status.

\code{rb_taxonomy()} requires all seven taxonomic ranks (\code{kingdom} through
\code{species}) to be present in the table. The columns \code{occurrence} and
\code{habitat} are included in the output if they exist in the table.
}
\examples{
\dontrun{
con <- rb_connect()

# Get all passing COI sequences
coi <- rb_get_sequences(con, marker = "COI")

# List all species with sequence counts
taxa <- rb_list_taxa(con, rank = "species")

# Get taxonomy for a specific species
tax <- rb_taxonomy(con, species = "Danio rerio")

# Filter an in-memory data frame
filtered <- rb_filter_reference(refs, marker = "12S", min_length = 100)

rb_disconnect(con)
}

}
\concept{database queries}
