% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taxonomy.R
\name{rb_taxonomy_utils}
\alias{rb_taxonomy_utils}
\alias{rb_build_taxonomy_string}
\alias{rb_standardize_species}
\alias{rb_join_taxonomy}
\title{Taxonomy standardization, joining, and string formatting}
\usage{
rb_build_taxonomy_string(data, style = c("plain", "rank_prefix"))

rb_standardize_species(
  names,
  reference_table,
  name_col = "alternative_clean",
  valid_col = "valid_name",
  taxonomy_lookup = NULL
)

rb_join_taxonomy(
  data,
  taxonomy_table,
  by = "species",
  defaults = list(),
  unassigned_label = "UNASSIGNED"
)
}
\arguments{
\item{data}{A data frame containing at least the seven taxonomic
ranks (\code{kingdom} through \code{species}) for \code{rb_build_taxonomy_string()},
or at least the column specified by \code{by} for \code{rb_join_taxonomy()}.}

\item{style}{Formatting style for \code{rb_build_taxonomy_string()}.
One of \code{"plain"} (default) or \code{"rank_prefix"}.}

\item{names}{Character vector of species names to standardize.}

\item{reference_table}{A data frame mapping alternative names to
valid names for \code{rb_standardize_species()}.}

\item{name_col}{Column in \code{reference_table} holding alternative/known
names. Default \code{"alternative_clean"}.}

\item{valid_col}{Column in \code{reference_table} holding the valid name.
Default \code{"valid_name"}.}

\item{taxonomy_lookup}{Optional callback \verb{function(name) -> valid_name}
used when \code{reference_table} has no match (e.g., an external catalog
API). \code{NULL} by default.}

\item{taxonomy_table}{A data frame containing taxonomic lineage to join
onto \code{data} for \code{rb_join_taxonomy()}. Must contain the column specified by \code{by}, and optionally
columns for \code{genus}, \code{family}, \code{order}, and higher ranks.}

\item{by}{Column name to join on. Default \code{"species"}.}

\item{defaults}{Named list of constant higher-rank values applied to
every row, e.g., \code{list(kingdom = "Metazoa", phylum = "Chordata")}.}

\item{unassigned_label}{Label used when genus/family/order cannot be
resolved. Default \code{"UNASSIGNED"}.}
}
\value{
\itemize{
\item \code{rb_build_taxonomy_string()} returns a character vector of formatted
taxonomy strings, one per row.
\item \code{rb_standardize_species()} returns a character vector of standardized
species names, with \code{NA} for unmatched names.
\item \code{rb_join_taxonomy()} returns the input data frame with taxonomic
lineage columns joined.
}
}
\description{
Functions to standardize species names, join taxonomic lineage onto
reference data, and format taxonomy strings for export.
\itemize{
\item \code{rb_build_taxonomy_string()}: Formats the seven taxonomic ranks into
a semicolon-delimited string, optionally with rank prefixes.
\item \code{rb_standardize_species()}: Maps alternative species names to valid
names using a reference table, with an optional external taxonomy
lookup callback as fallback.
\item \code{rb_join_taxonomy()}: Joins a taxonomy table onto reference data,
filling missing genus/family/order with a configurable label and
applying constant higher-rank defaults.
}
}
\details{
\code{rb_build_taxonomy_string()} supports two styles:\tabular{ll}{
   Style \tab Example output \cr
   \code{"plain"} \tab \verb{Animalia;Chordata;Actinopterygii;...} \cr
   \code{"rank_prefix"} \tab \verb{k__Animalia;p__Chordata;c__Actinopterygii;...} \cr
}


Missing or empty values are replaced with \code{"unassigned"}.

\code{rb_join_taxonomy()} guards against silent Cartesian joins by checking
for duplicate keys in \code{taxonomy_table} before merging. If duplicates
are found, it stops with an informative error.
}
\examples{
\dontrun{
# Build taxonomy strings for DADA2 export
tax_strings <- rb_build_taxonomy_string(refs, style = "plain")

# Standardize species names
valid_names <- rb_standardize_species(
  raw_names,
  reference_table = name_mapping
)

# Join taxonomy onto curated data
final_data <- rb_join_taxonomy(
  qc_data,
  taxonomy_table = tax_table,
  defaults = list(kingdom = "Animalia", phylum = "Chordata")
)
}

}
\concept{taxonomy}
