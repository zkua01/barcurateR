% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{rb_ambiguity_utils}
\alias{rb_ambiguity_utils}
\alias{rb_detect_ambiguity}
\alias{rb_write_ambiguity_csv}
\alias{rb_read_ambiguity_csv}
\title{Detect and archive ambiguous sequences}
\usage{
rb_detect_ambiguity(data, species_col = "species", sequence_col = "sequence")

rb_write_ambiguity_csv(data, path = "ambiguous_sequences.csv")

rb_read_ambiguity_csv(path = "ambiguous_sequences.csv")
}
\arguments{
\item{data}{A standardized data frame containing at least the columns
specified by \code{species_col} and \code{sequence_col}.}

\item{species_col}{Name of the species column. Default \code{"species"}.}

\item{sequence_col}{Name of the sequence column. Default \code{"sequence"}.}

\item{path}{Path to the ambiguity CSV file. Default
\code{"ambiguous_sequences.csv"}.}
}
\value{
\itemize{
\item \code{rb_detect_ambiguity()} returns the input data frame with an
\code{is_ambiguous} logical column added.
\item \code{rb_write_ambiguity_csv()} returns the output path invisibly.
\item \code{rb_read_ambiguity_csv()} returns a data frame.
}
}
\description{
Utility functions for detecting sequences assigned to multiple species
and archiving ambiguity records for downstream auditing.
\itemize{
\item \code{rb_detect_ambiguity()}: Flags sequences that are assigned to more
than one species.
\item \code{rb_write_ambiguity_csv()}: Writes an ambiguity table to CSV.
\item \code{rb_read_ambiguity_csv()}: Reads an ambiguity table from CSV.
}
}
\details{
Ambiguous sequences are sequences that occur under more than one
species name. These should normally be resolved before QC using
\code{rb_resolve_ambiguous()} or a manual resolution table.

The ambiguity table is an auditing artifact. It is not used directly
by the QC pipeline, but it can be archived in the final SQLite
database by \code{rb_curate_reference()} using the \code{ambiguity_data} or
\code{ambiguity_path} arguments.
}
\examples{
\dontrun{
combined <- rb_combine_sources(list(ncbi_parsed, bold_parsed))

combined <- rb_detect_ambiguity(combined)

ambiguities <- combined[combined$is_ambiguous, ]

if (nrow(ambiguities) > 0) {
  rb_write_ambiguity_csv(ambiguities, "ambiguous_sequences.csv")
}
}

}
\seealso{
Other parsing:
\code{\link{rb_combine_resolve}},
\code{\link{rb_parse_sources}},
\code{\link{rb_sequence_prep}}
}
\concept{parsing}
