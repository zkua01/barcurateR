% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db-write.R
\name{rb_write_db}
\alias{rb_write_db}
\alias{rb_write_reference_table}
\alias{rb_write_qc_table}
\alias{rb_write_barcode_gap_table}
\alias{rb_write_ambiguity_table}
\alias{rb_build_reference_db}
\title{Write curated reference data to a SQLite database}
\usage{
rb_write_reference_table(
  con,
  data,
  table_name = "reference_final",
  overwrite = TRUE,
  require_taxonomy = TRUE
)

rb_write_qc_table(con, data, table_name = "qc_reference", overwrite = TRUE)

rb_write_barcode_gap_table(
  con,
  data,
  table_name = "barcode_gap_metrics",
  overwrite = TRUE
)

rb_write_ambiguity_table(
  con,
  data,
  table_name = "ambiguous_sequences",
  overwrite = TRUE
)

rb_build_reference_db(
  path,
  reference_data,
  qc_data = NULL,
  barcode_gap_data = NULL,
  ambiguity_data = NULL,
  reference_table = "reference_final",
  qc_table = "qc_reference",
  barcode_gap_table = "barcode_gap_metrics",
  ambiguity_table = "ambiguous_sequences",
  overwrite = TRUE,
  require_taxonomy = TRUE
)
}
\arguments{
\item{con}{A \code{DBIConnection} object to an open SQLite database.}

\item{data}{A data frame to write to the database.}

\item{table_name}{Name of the table to write.}

\item{overwrite}{Logical. Overwrite existing tables? Default \code{TRUE}.}

\item{require_taxonomy}{Logical. If \code{TRUE}, enforces that the reference
table contains all seven taxonomic ranks (kingdom through species)
before writing. Default \code{TRUE}.}

\item{path}{Path to the SQLite database file to create or overwrite.}

\item{reference_data}{Data frame of final curated reference sequences.}

\item{qc_data}{Optional data frame of QC results.}

\item{barcode_gap_data}{Optional data frame of barcode gap metrics.}

\item{ambiguity_data}{Optional data frame of ambiguity records.}

\item{reference_table}{Name for the reference table (default \code{"reference_final"}).}

\item{qc_table}{Name for the QC table (default \code{"qc_reference"}).}

\item{barcode_gap_table}{Name for the barcode gap table (default \code{"barcode_gap_metrics"}).}

\item{ambiguity_table}{Name for the ambiguity table (default \code{"ambiguous_sequences"}).}
}
\value{
\itemize{
\item \code{rb_build_reference_db()} returns the normalized path to the created
database file (invisibly).
\item The individual \verb{rb_write_*} functions return \code{TRUE} invisibly on success.
}
}
\description{
Functions to write curated reference sequences, QC results, barcode gap
metrics, and ambiguity records into a SQLite database. These functions
do \strong{not} filter data — downstream filtering is handled by
\code{\link[=rb_get_sequences]{rb_get_sequences()}}, \code{\link[=rb_filter_reference]{rb_filter_reference()}}, and export functions.
\itemize{
\item \code{rb_build_reference_db()}: One-step builder that creates a new SQLite
file and writes all supplied tables in a single call.
\item \code{rb_write_reference_table()}: Writes the main reference sequence table.
\item \code{rb_write_qc_table()}: Writes the QC flag table.
\item \code{rb_write_barcode_gap_table()}: Writes barcode gap metrics.
\item \code{rb_write_ambiguity_table()}: Writes the pre-QC ambiguity archive.
}
}
\details{
The default table names are:\tabular{ll}{
   Table \tab Default name \cr
   Reference sequences \tab \code{reference_final} \cr
   QC results \tab \code{qc_reference} \cr
   Barcode gap metrics \tab \code{barcode_gap_metrics} \cr
   Ambiguity archive \tab \code{ambiguous_sequences} \cr
}


Before writing, \code{rb_prepare_reference_table()} ensures that the following
columns exist (adding defaults where missing):
\itemize{
\item \code{qc_flag} (default \code{"pass"})
\item \code{source} (default \code{"unknown"})
\item \code{seq_type} (standardized from \code{gene} or \code{marker} columns)
\item \code{unique_code} (auto-generated from deduplicated sequences)
}

List-columns are rejected and factor columns are converted to character
before writing.
}
\examples{
\dontrun{
# Build a complete database in one call
rb_build_reference_db(
  path = "my_reference.db",
  reference_data = final_data,
  qc_data = qc_results,
  barcode_gap_data = gap_metrics
)

# Or write tables individually to an existing connection
con <- DBI::dbConnect(RSQLite::SQLite(), "my_reference.db")
rb_write_reference_table(con, final_data)
rb_write_qc_table(con, qc_results)
DBI::dbDisconnect(con)
}

}
\concept{database writing}
