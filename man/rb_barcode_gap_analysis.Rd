% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gap.R
\name{rb_barcode_gap_analysis}
\alias{rb_barcode_gap_analysis}
\alias{rb_diagnostic_sites}
\alias{rb_barcode_gap_species}
\alias{rb_run_barcode_gap}
\title{Barcode gap analysis for curated reference databases}
\usage{
rb_diagnostic_sites(focal_seqs, other_seqs, min_freq = 0.9, max_other = 500)

rb_barcode_gap_species(
  species_name,
  data,
  aligned_seqs,
  seq_len,
  species_col = "species",
  genus_col = "genus",
  marker = NA_character_,
  min_seqs = 2,
  max_other = 2000,
  max_congeners = 500
)

rb_run_barcode_gap(
  data,
  markers = c("COI", "12S", "16S", "genome"),
  species_col = "species",
  genus_col = NULL,
  gene_col = "seq_type",
  sequence_col = "sequence",
  qc_flag_col = "qc_flag",
  min_seqs = 5,
  parallel = TRUE,
  n_workers = NULL,
  remove_gaps = TRUE
)
}
\arguments{
\item{focal_seqs}{Character vector of focal sequences for
\code{rb_diagnostic_sites()}.}

\item{other_seqs}{Character vector of non-focal sequences for
\code{rb_diagnostic_sites()}.}

\item{min_freq}{Minimum focal base frequency required for a position to
be considered diagnostic. Default \code{0.9}.}

\item{max_other}{Maximum number of non-focal sequences to use when
computing global inter-specific distances. Default \code{2000}.}

\item{species_name}{Species to analyze in \code{rb_barcode_gap_species()}.}

\item{data}{A standardized reference data frame.}

\item{aligned_seqs}{Named character vector of aligned sequences.}

\item{seq_len}{Aligned sequence length used to normalize distances.}

\item{species_col}{Name of the species column. Default \code{"species"}.}

\item{genus_col}{Name of the genus column. If \code{NULL}, genus is derived
from the first token of the species name.}

\item{marker}{Marker name associated with the aligned sequences.}

\item{min_seqs}{Minimum number of sequences required per species.}

\item{max_congeners}{Maximum number of congeneric sequences to use when
computing congeneric distances. Default \code{500}.}

\item{markers}{Character vector of marker names to analyze.}

\item{gene_col}{Name of the marker/gene column. Default \code{"seq_type"}.}

\item{sequence_col}{Name of the sequence column. Default \code{"sequence"}.}

\item{qc_flag_col}{Name of the QC flag column. Default \code{"qc_flag"}.
If present, only sequences with \code{qc_flag == "pass"} are analyzed.}

\item{parallel}{Logical. If \code{TRUE}, uses a parallel cluster where possible.}

\item{n_workers}{Optional number of parallel workers. If \code{NULL}, defaults
to \code{parallel::detectCores() - 2}.}

\item{remove_gaps}{Logical. If \code{TRUE}, removes \code{"-"} characters from
sequences before analysis. Default \code{TRUE}.}
}
\value{
\itemize{
\item \code{rb_run_barcode_gap()} returns a data frame of barcode gap metrics,
with one row per species per marker. Returns an empty data frame if
no suitable data are available.
\item \code{rb_barcode_gap_species()} returns a one-row data frame of barcode
gap metrics, or \code{NULL} if the species does not meet the minimum
sequence threshold or has no non-focal comparison sequences.
\item \code{rb_diagnostic_sites()} returns an integer count of diagnostic sites.
}
}
\description{
Functions to assess whether reference sequences for a species form a
distinguishable barcode gap relative to sequences from other species.
\itemize{
\item \code{rb_run_barcode_gap()}: Runs barcode gap analysis across one or more
markers. Aligns sequences with \code{DECIPHER::AlignSeqs()}, then computes
per-species distance metrics.
\item \code{rb_barcode_gap_species()}: Computes barcode gap metrics for a single
species within an aligned marker dataset.
\item \code{rb_diagnostic_sites()}: Counts fixed diagnostic nucleotide positions
that distinguish focal sequences from non-focal sequences.
}
}
\details{
\code{rb_run_barcode_gap()} expects a standardized reference data frame with
at least species, marker/gene, and sequence columns. If a \code{qc_flag}
column is present, only sequences with \code{qc_flag == "pass"} are used.

For each marker, the function:
\enumerate{
\item Filters to the requested marker.
\item Optionally removes alignment gap characters (\code{"-"}).
\item Filters species with at least \code{min_seqs} sequences.
\item Aligns sequences using \code{DECIPHER::AlignSeqs()}.
\item Computes barcode gap metrics for each species.
}

The returned data frame includes intra- and inter-specific distance
summaries, gap metrics, congeneric comparisons where available,
diagnostic site counts, sampling adequacy, taxonomic resolution,
assignment risk, and a recommended similarity threshold.

If \code{parallel = TRUE}, a parallel cluster is used for the per-species
calculations. If cluster setup or execution fails, the function
automatically falls back to serial execution for that marker.

Distance calculations use \code{rb_string_dist()}, which prefers
\code{pwalign::stringDist()} and falls back to \code{Biostrings::stringDist()}
on older Biostrings versions.
}
\section{Output columns}{

The data frame returned by \code{rb_run_barcode_gap()} and
\code{rb_barcode_gap_species()} includes:
\itemize{
\item \code{species}, \code{marker}, \code{n_sequences}
\item \code{max_intra}, \code{min_inter}
\item \code{gap_exists}, \code{gap_width}
\item \code{p_overlap}, \code{q_overlap}
\item \code{median_intra}, \code{median_inter}
\item \code{intra_95}, \code{inter_5}, \code{percentile_gap}, \code{robust_gap}
\item \code{n_congeners_in_db}, \code{min_congeneric}, \code{congeneric_gap}
\item \code{congeneric_p_overlap}, \code{congeneric_q_overlap}
\item \code{n_diagnostic_sites}
\item \code{sampling_adequacy}, \code{taxonomic_resolution}
\item \code{gap_may_be_undersampling_artifact}
\item \code{error_msg}
\item \code{assignment_risk}, \code{recommended_threshold}
}
}

\examples{
\dontrun{
gap_results <- rb_run_barcode_gap(
  final_data,
  markers = c("COI", "12S"),
  min_seqs = 5,
  parallel = FALSE
)

head(gap_results)
}

}
\concept{barcode gap}
