% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{rb_diagnostics}
\alias{rb_diagnostics}
\alias{rb_marker_coverage}
\alias{rb_source_coverage}
\alias{rb_qc_summary}
\alias{rb_barcode_gap}
\alias{rb_ambiguity}
\alias{rb_read_barcode_gap}
\alias{rb_read_ambiguity}
\title{Audit and inspect a curated reference database}
\usage{
rb_marker_coverage(con, rank = "species", table_name = "reference_final")

rb_source_coverage(con, table_name = "reference_final")

rb_qc_summary(
  con,
  table_name = "reference_final",
  qc_table_name = "qc_reference"
)

rb_barcode_gap(
  con,
  species = NULL,
  marker = NULL,
  table_name = "barcode_gap_metrics"
)

rb_ambiguity(con, table_name = "ambiguous_sequences")

rb_read_barcode_gap(path = NULL, species = NULL, marker = NULL)

rb_read_ambiguity(path = NULL, action = NULL)
}
\arguments{
\item{con}{A \code{DBIConnection} object to an open barcurateR SQLite database.}

\item{rank}{Taxonomic rank to group by: \code{"species"}, \code{"genus"}, \code{"family"},
or \code{"order"}. Default \code{"species"}.}

\item{table_name}{Name of the reference table to query (default \code{"reference_final"}).}

\item{qc_table_name}{Name of the QC table to query (default \code{"qc_reference"}).}

\item{species}{Optional character vector of species names to filter by.}

\item{marker}{Optional character vector of marker names to filter by.}

\item{path}{Path to a CSV file. If \code{NULL}, a default bundled file is used.}

\item{action}{Optional filter for ambiguity resolution actions.}
}
\value{
\itemize{
\item \code{rb_marker_coverage()} returns a data frame with columns for the requested
rank, \code{seq_type}, \code{n_sequences}, and \code{n_sources}.
\item \code{rb_source_coverage()} returns a data frame with \code{source}, \code{seq_type},
\code{n_sequences}, and \code{n_species}.
\item \code{rb_qc_summary()} returns a data frame with \code{qc_flag} and \code{n_sequences},
ordered by count descending.
\item \code{rb_barcode_gap()} and \code{rb_read_barcode_gap()} return a data frame of
barcode gap metrics.
\item \code{rb_ambiguity()} and \code{rb_read_ambiguity()} return a data frame of
ambiguity records.
}
}
\description{
Functions to generate diagnostic summaries and inspect the contents
of a curated reference database. These functions help users assess
data quality, marker coverage, source representation, and potential
issues before using the database for taxonomic assignment.

\strong{Database query functions} (require an open SQLite connection):
\itemize{
\item \code{rb_marker_coverage()}: Summarises sequences per taxonomic rank and marker.
\item \code{rb_source_coverage()}: Summarises sequences per source and marker.
\item \code{rb_qc_summary()}: Summarises QC flag distributions.
\item \code{rb_barcode_gap()}: Retrieves barcode gap metrics from the database.
\item \code{rb_ambiguity()}: Retrieves archived ambiguity records from the database.
}

\strong{File reader functions} (read from CSV files):
\itemize{
\item \code{rb_read_barcode_gap()}: Reads barcode gap metrics from a CSV file.
\item \code{rb_read_ambiguity()}: Reads ambiguity records from a CSV file.
}
}
\details{
All database query functions filter on \code{qc_flag = 'pass'} for coverage
functions (\code{rb_marker_coverage}, \code{rb_source_coverage}), ensuring only
passing sequences are counted. The \code{rb_qc_summary()} function reports
all QC flags without filtering.

If the requested table does not exist in the database, \code{rb_barcode_gap()}
and \code{rb_ambiguity()} return an empty data frame rather than throwing an
error, allowing graceful degradation when optional tables are absent.
}
\examples{
\dontrun{
con <- rb_connect()

# Marker coverage by species
rb_marker_coverage(con, rank = "species")

# Source coverage
rb_source_coverage(con)

# QC summary
rb_qc_summary(con)

# Barcode gap metrics for a specific marker
rb_barcode_gap(con, marker = "COI")

# Ambiguity records
rb_ambiguity(con)

rb_disconnect(con)
}

}
\concept{diagnostics}
